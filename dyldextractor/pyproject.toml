[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dyldextractor"
version = "1.0.0"
description = "A tool to extract dyld shared cache files"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["dyldextractor"]