# Moved to Rust

JSpace lives in `rust/ghidra-vibe-tools` (`ghidra-vibe-jspace`, `ghidra-vibe-rag-mcp`).

The Python package under this directory is deprecated and will be removed.
Use `scripts/ghidra-vibe-jspace` (wrapper) or `nix build .#ghidra-vibe-tools`.
