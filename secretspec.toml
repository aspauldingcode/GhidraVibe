# Secret *names* only — never commit values.
# API/cloud LLM access is opt-in via Nix `agent.apiKeyFile` or GUI; not shipped.
# Provision keys with your private vault (pass / SecretSpec) outside this package.

[project]
name = "ghidra-vibe"

[profiles.default]
openai_api_key = { description = "Optional OpenAI-compatible API key (user file / SecretSpec; never bundled)" }
anthropic_api_key = { description = "Optional Anthropic API key (user file / SecretSpec; never bundled)" }
